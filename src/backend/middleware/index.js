const verifyToken = async (req, res) => {
  res.send('Verificacion de token');
};

const verifyEmail = async (req, res) => {
  res.send('Verificacion de token');
};

module.exports = {
  verifyToken,
  verifyEmail,
};
